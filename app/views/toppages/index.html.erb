<% if logged_in? %>
      <%= render 'plans/plans', plans: @plans %>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>Welcome to the TripBooks</h1>
      <%= link_to 'Sign up now!', signup_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>