<div class="offset-sm-4 col-sm-4">
  <h1 class="text-center">旅程の編集</h1>
  <%= render 'form' %>
  <p class="mt-3"><%= link_to "削除", plan_path(@plan), method: :delete, data: { confirm: "You sure?" }, class: 'btn btn-danger btn-sm' %>
  <p class="mt-3"><%= link_to 'スケジュールに戻る', plan_path, class: "text-danger" %></p>
</div>